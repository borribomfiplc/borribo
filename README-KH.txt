QR validation patch

Copy all files/folders inside this patch into your BORRIBO project root and choose Replace.
Then run:
  npm run deploy:telegram
  npm run build
  npm run deploy

After deploy, hard refresh with Ctrl+Shift+R.
In Admin > GPS និង QR, ensure "តម្រូវឲ្យស្កេន QR" is ON, then click Save.
